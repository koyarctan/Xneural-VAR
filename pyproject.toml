[project]
name = "xneural-var"
version = "0.1.0"
description = "Modular GVAR with Neural Granger Causality regularization."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.23",
  "torch>=2.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
